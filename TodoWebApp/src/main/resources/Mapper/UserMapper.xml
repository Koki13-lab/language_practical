<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.forgeon.todo.mapper.UserMapper">

	<select id="findByName" resultType="com.forgeon.todo.dto.User">
		SELECT * FROM users WHERE mail = #{name}
	</select>

	<select id="getUsers" resultType="com.forgeon.todo.dto.User">
		SELECT id,name,mail,password FROM users WHERE deleted != true ORDER BY
		id ASC
	</select>

	<select id="searchUsers" resultType="com.forgeon.todo.dto.User">
		SELECT id,name,mail FROM users WHERE name LIKE CONCAT('%', #{name}, '%')
		AND deleted != true ORDER BY id ASC
	</select>

	<select id="getUserInfor" resultType="com.forgeon.todo.dto.User">
		SELECT id,name,mail,role,remarks FROM users WHERE id = #{id} AND deleted
		!= true
	</select>

	<select id="existsActiveUser" resultType="boolean">
		SELECT EXISTS (SELECT 1 FROM users
		WHERE id = #{id}
		AND deleted = false
		)
	</select>

	<update id="updateUser" parameterType="com.forgeon.todo.dto.User">
		UPDATE users SET
		name=#{name},mail=#{mail},role=#{role},remarks=#{remarks},updated_at=#{updatedAt},updated_by=#{updatedBy}
		WHERE id=#{id}
	</update>

	<insert id="insertUser" parameterType="com.forgeon.todo.dto.User">
		INSERT INTO users (name,password,mail,role,remarks,created_by)
		VALUES(#{name},#{password},#{mail},#{role},#{remarks},#{createdBy})
	</insert>

	<update id="deleteUser" parameterType="com.forgeon.todo.dto.User">
		UPDATE users SET
		deleted_at = #{deletedAt},deleted_by = #{deletedBy},deleted = #{deleted}
		Where id = #{id}
	</update>

</mapper>